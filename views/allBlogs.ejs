<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogs</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-50 font-sans">

    <header class="flex flex-wrap justify-between items-center max-w-7xl mx-auto p-6">
        <div class="text-3xl font-extrabold text-green-600">
            <a href="/">VMBlog</a>
        </div>
        <nav class="hidden md:flex space-x-6 text-gray-700 text-base font-medium">
            <a href="#" class="hover:text-green-600 transition">Community</a>
            <a href="#" class="hover:text-green-600 transition">Podcasts</a>
            <a href="#" class="hover:text-green-600 transition">Courses</a>
            <a href="#" class="hover:text-green-600 transition">Learn</a>
            <a href="#" class="hover:text-green-600 transition">Tools</a>
        </nav>
        <div class="text-sm text-gray-600">
            <% if (user) { %>
                <a href="/logout" class="text-red-600 hover:text-red-800 transition">LOGOUT</a>
                <% } else { %>
                    <a href="/login" class="hover:text-green-600 transition">LOGIN</a>
                    <% } %>
        </div>
    </header>

    <div class="max-w-4xl mx-auto px-4 py-10">
        <h1 class="text-3xl font-bold text-gray-800 mb-8 text-center">Latest Blogs</h1>

        <div class="space-y-6">
            <% posts.forEach(post=> { %>
                <div class="bg-white p-6 rounded-xl shadow-md border border-gray-200">
                    <div class="flex justify-between items-center text-sm text-gray-500 mb-2">
                        <p class="font-medium text-gray-700">
                            <%= post.user.username %>
                        </p>
                        <p>
                            <%= new Date(post.createdAt).toDateString() %>
                        </p>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-2">
                        <%= post.title %>
                    </h3>
                    <p class="text-gray-600">
                        <%= post.content %>
                    </p>
                </div>
                <% }) %>
        </div>

    </div>

</body>

</html>